# PDF parser / preprocessing configuration

# Directory paths (override if you keep data elsewhere)
# RAW_DATA_DIR=./data/raw
# PROCESSED_PDF_DIR=./data/parsed
# LOG_DIR=./logs/pdf_parser

# Markdown page delimiter inserted between pages
# PAGE_DELIMITER="\n\n---\n\n"

# OCR behavior: auto | off | force
#   auto  -> only OCR pages with no native text
#   off   -> never run OCR
#   force -> combine native + OCR text
OCR_STRATEGY=auto

# Path to tesseract executable (set if auto-detect fails)
# TESSERACT_CMD=/opt/homebrew/bin/tesseract
